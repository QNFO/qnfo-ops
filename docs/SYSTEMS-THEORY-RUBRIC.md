# Systems Theory Rubric - QNFO Objective Function

Canonical reference for the System-Level Report Card (docs/FLEET-REPORT-CARD.md, generated by scripts/fleet-report-card.py).
Last updated 2026-09-10.

## 1. The rubric stack: named frameworks applied to QNFO

| Framework | What it measures | QNFO operationalization | Current reading |
|---|---|---|---|
| **Sheridan-Verplank Levels of Automation (1-10)** | Allocation of function between human and computer | Autonomy ladder L0-L3 (AF-1 architecture): observe -> propose -> act-with-receipts -> extended autonomy | **L2** (act-with-receipts); LOA ~7 (computer executes, then informs). L3 promotion requires N clean verified cycles + tested kill-switch + tested rollback |
| **Parasuraman 4-stage model** | Information acquisition -> analysis -> decision selection -> action | Automated probes/telemetry (acq), audits + chain health (analysis), LLM/agent decisions under human-set objectives (select), receipted automation (act) | 0.95 / 0.70 / **0.40** / 0.90 - selection is the weak stage: objectives come from the user, decisions from agent sessions |
| **Beer Viable System Model (S1-S5)** | Viability = 5 subsystems in recursive balance | S1 = domain pipelines (research/publish/outreach), S2 = schedulers + intent-orchestrator + queues, S3 = kaizen + guards + auditors, S4 = radars + idea-factory (environment scan), S5 = system prompt + gates + register (policy/identity) | S3 is strongest; **S4->S3 coupling is weak** (radars feed ideas but the control loop does not close on them) |
| **Ashby Law of Requisite Variety** | Controller variety must match controlled-system variety | Control layer (weekly kaizen + daily audits) vs operations layer (78 workers, sub-hourly crons) | **Variety gap** - the control layer has far less variety than operations; the L4 continuous decide-loop + per-chain controllers are the variety engineering |
| **Boyd OODA loop** | Observe-orient-decide-act cycle time | Probes 15-min, audits daily, kaizen weekly, decide-loop designed | Multi-timescale OODA; the minutes-scale loop (L4) is the missing rung |
| **Chaos theory: sensitivity to initial conditions** | Small cause -> large effect in coupled systems | DR runbook (change-audit-first, differential proof, rollback) - the 08-10 email cascade and 09-04 config saga are canonical basin-crossings | Discipline is rollback-centric, not prediction-centric |
| **Chaos theory: attractors and basins** | Long-run states the system falls into | L3 set-points (cost <$90/30d, errors <0.1%, user-wait == 0) + kill-switches as basin walls | Pathological attractors exist ("zero errors -> do nothing", "activity -> spam"); walls are the tested kill-switches |
| **Contraction mappings (dynamical stability)** | Write paths must converge, not diverge | Idempotency keys, exact-dedupe (intent-orchestrator), h32 hash dedupe (auto-scan), publish locks, TITLE-EXISTENCE checks | Duplicate-record races (two concepts for one paper) were literally diverging trajectories; dedupe is the contraction |
| **Edge of chaos (Langton)** | Productive regime between order and disorder | Bounded mutation (canary-gated) + strong selection (verified cycles, rollback) | Fleet was over-ordered ("watchmaker"); the L4 loop tunes toward the productive edge |
| **Recursive self-improvement (RSI)** | Candidate -> change -> verify closure, fully internal | kaizen dispositions, guard suite, paper-reviser loop, fleet-deploy CAN redeploy | 0.15: no autonomous code mutation with verified selection yet |
| **METR autonomy evals / task-ability** | Time-bounded autonomous task completion | Not yet run | Unmeasured |
| **ARC-AGI** | Fluid, novel reasoning (intelligence benchmark) | Not yet run (design: 10-task JSON-grid probe via fleet-executor AI tasks) | Unmeasured - registered row |
| **GAIA / SWE-bench Verified / OSWorld** | Assistant task completion / agentic coding / computer use | Not yet run | Unmeasured |

## 2. The objective function (SAI - System Autonomy Index)

SAI = 0.30*autonomy + 0.15*thinking + 0.15*decision + 0.15*self_improv + 0.10*reliability + 0.10*integration + 0.05*governance

- **autonomy**: Watchmaker Index (user-waiting rows + loop health); CAPPED at 0.70 by the ladder (L2). L3 promotion raises the cap to 0.85; L4 removes it.
- **thinking**: 50% adversarial-disconfirmation gates (live, 11/11) + 50% standardized benchmarks (0 until measured).
- **decision**: Parasuraman four-stage mean.
- **self_improv**: 30% kaizen closure + 20% guard-suite hea

## 6. L3 promotion gate (rc-193) - the ladder cap 0.70 -> 0.85

The autonomy dimension is capped at 0.70 while the fleet is L2 (act-with-receipts). Promotion to L3 requires ALL of:

1. **Tested kill-switch** - pipeline_flags.report_card_autonomy (off = generate-only; on = auto-commit + push). TESTED 2026-09-10.
2. **Tested rollback** - the auto-commit is one deterministic commit; git revert undoes it. TESTED 2026-09-10 (5d995d1 -> d7682da).
3. **N consecutive clean weekly cycles (N=3)** - a clean cycle = the Monday 06:30 cloud cron (report-card-weekly-cron) fires, the executor run status = ok (fleet_runs), the report_card_history row + cloud_ops_events marker land, and the host regen (scripts/report-card-autonomy.py) commits without error.
4. **No open HARD finding** against the report-card loop.

Current status (2026-09-10): kill-switch TESTED, rollback TESTED, clean cycles = 0/3 (first Monday-gate 2026-09-14). => NOT PROMOTED; the cap stays 0.70 until 3 clean cycles complete (~2026-09-28).
